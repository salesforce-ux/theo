<?xml version="1.0" encoding="utf-8"?>
<styles>
{{#each categories}}
<!-- {{name}} -->
{{#each properties}}
{{#if comment}}<!-- {{comment}}-->{{/if}}
    <variable name="{{name}}" value="{{value}}" />{{/each}}
{{/each}}
</styles>