{
  "properties": [
  {{#each properties}}
    {
      "name": "{{camelCase name}}",
      "value": "{{iosValue}}",
      "category": "{{category}}"
    }{{#unless @last}},{{/unless}}
  {{/each}}
  ]
}