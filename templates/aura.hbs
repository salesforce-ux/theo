<!-- This file is being generated. Please reach out to jlevine@salesforce.com or nmcwilliams@salesforce.com
     if you need to do any changes. Changes are maintained here: https://git.soma.salesforce.com/ux/s1variables -->
<aura:theme {{#if extends}}extends="{{extends}}"{{/if}}>
  {{#each imports}}
  <aura:importTheme name="{{this}}"/>{{/each}}
  {{#each properties}}
  {{#if comment}}<!-- {{comment}} -->{{/if}}
  <aura:var name="{{#camelCase}}{{name}}{{/camelCase}}" value="{{value}}" />{{/each}}
</aura:theme>